{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd BLE && npm install && npm run build",
  "outputDirectory": "BLE/dist",
  "installCommand": "cd BLE && npm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
